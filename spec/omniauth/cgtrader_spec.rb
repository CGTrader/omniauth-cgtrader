RSpec.describe Omniauth::Cgtrader do
  it "has a version number" do
    expect(Omniauth::Cgtrader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
